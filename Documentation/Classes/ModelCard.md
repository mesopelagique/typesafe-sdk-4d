# ModelCard

One entry of `GET /v1/models`.

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `name` | Text | The model name, as passed to `model`. |
| `description` | Text | What the model is for. |
| `releaseDate` | Text | Its release date. Read from `releaseDate` or `release_date`. |
